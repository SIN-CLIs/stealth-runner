# banned.md – ABSOLUTE VERBOTE

- ❌ `cua-driver` — ersetzt durch `skylight-cli`
- ❌ `open -na "Google Chrome"` — nur `playstealth-cli launch`
- ❌ `AXStaticText` klicken — nur interaktive Rollen
- ❌ Blindes Raten von Element-IDs — Vision MUSS vor jedem Klick laufen
- ❌ DOM-Prescan als Ersatz für Vision — klickt blind dasselbe Element
- ❌ CDP, DOM, Chrome-Extensions — sofort detektierbar
- ❌ `.env` committen — Credential-Leak
- ❌ `--x`/`--y` Koordinaten raten — trifft das Apple-Menü
- ❌ `CGEventPostToPid` — TOT auf Chrome 148/macOS 26
